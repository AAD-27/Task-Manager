name: task_manager_app
description: A Flutter Task Manager App integrated with Back4App (Parse Server) - CRUD Application with user authentication and cloud database.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  parse_server_sdk_flutter: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

